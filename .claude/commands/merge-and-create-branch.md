Commit any changes in the current branch and provide a suitable commit message based on the code changes. Than merge the current branch into the main branch and reslove any issues off the back of that merge. Than create a new branch called $ARGUMENTS. and make the remote branch on Github if there is reflect the same changes. And don't forgate to remove the merged branchs local and in remote repo.